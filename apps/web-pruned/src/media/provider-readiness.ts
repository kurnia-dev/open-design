// Reference: apps/web/src/media/provider-readiness.ts
export function isMediaProviderPickerReady(providerId: string, config: any): boolean { return false; }
export function isMediaModelPickerReady(modelId: string, config: any): boolean { return false; }
export function isMediaProviderReady(providerId: string, config: any): boolean { return false; }
